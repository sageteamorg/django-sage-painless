"""
Auto Generated admin.py
You may need to change some parts
"""
from django.contrib import admin
from django.db.models import CharField, DateField, DateTimeField
from {{app_name}}.models import *

{% for model in models %}
@admin.register({{model}})
class {{model}}Admin(admin.ModelAdmin):
    """
    {{model}} Admin
    Auto generated
    """
    list_display = [
        field.column for field in {{model}}._meta.get_fields() if type(field) == CharField
    ]

    list_filter = [
        field.column for field in {{model}}._meta.get_fields() if type(field) == DateField or type(field) == DateTimeField
    ]
{% endfor %}